# taxi-public-transit-integration

## Running

- `cd` into the src folder.
- Set up an osrm server. Follow https://github.com/Project-OSRM/osrm-backend
- Update the server url in `run_simulator.sh` file
- Run `bash run_simulator.sh`
